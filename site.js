async function loadProducts(){const root=document.getElementById('products');if(!root)return;try{const res=await fetch('/api/printful/products');const data=await res.json();if(!res.ok)throw new Error(data.error||'Unable to load products');const products=data.products||[];if(!products.length){root.innerHTML='<div class="loading">The Everlight Chronicles Website store is connected. Add products in Printful and they will appear here.</div>';return}root.innerHTML=products.map(p=>{const img=p.thumbnail_url||p.thumbnail||'';const name=p.name||'Everlight Merchandise';return `<article class="product">${img?`<img src="${img}" alt="${name.replace(/"/g,'&quot;')}">`:''}<h3>${name}</h3><p>Official Everlight Chronicles merchandise.</p></article>`}).join('')}catch(e){root.innerHTML='<div class="loading">Shop preview is ready. Printful connection will be verified on the Netlify preview deployment.</div>'}}loadProducts();